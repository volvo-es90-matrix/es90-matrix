name: ES90 reservations 09:00 slot

on:
  schedule:
    # 09:07-11:47 Asia/Seoul, every 20 minutes.
    - cron: "7,27,47 0-2 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    uses: ./.github/workflows/update-es90-reservations.yml
    with:
      target_hour: 9
    secrets: inherit
